from __future__ import annotations


def parse_code(code: str) -> str:
    """Parse code for execution.

    Args:
        code: Python code to parse.

    Returns:
        The code to execute (passed through as-is).
    """
    return code
